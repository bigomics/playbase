library(playbase)

## example files can be accessed via the example_file() function
counts <- read_counts(example_file("counts.csv"))
samples <- read_samples(example_file("samples.csv"))
contrasts <- read_contrasts(example_file("contrasts.csv"))

## create a pgx object (new style)
pgx <- create_pgx(counts, samples, contrasts)

## old style still works
pgx <- pgx.createPGX(counts, samples, contrasts)
