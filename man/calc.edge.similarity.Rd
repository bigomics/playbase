% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xcr-graph.r
\name{calc.edge.similarity}
\alias{calc.edge.similarity}
\alias{calc.edge.similarityMC}
\alias{calc.edge.similarityKFOLD}
\title{Calculate edge values from X
Calculate edge similarity}
\usage{
calc.edge.similarity(ee, X, nk = 4000, mc.cores = 1)

calc.edge.similarityMC(ee, X, nk = 5000, mc.cores = 4)

calc.edge.similarityKFOLD(ee, X, nk = 4000)
}
\arguments{
\item{ee}{Edge matrix}

\item{X}{Expression matrix}

\item{nk}{Number of edges per chunk. Default 4000.}

\item{mc.cores}{Number of cores for parallel processing. Default 1.}
}
\value{
Matrix of edge similarity values
}
\description{
Calculates similarity values for a graph edge matrix based on gene expression data.
}
\details{
This function takes an edge matrix \code{ee} and expression matrix \code{X}, and calculates similarity values for each edge.
It splits the edges into \code{nk} sized chunks and processes them in parallel using \code{mc.cores} cores.

For each edge, it extracts the linked genes, gets their expression profiles from \code{X}, and calculates correlation or distance.
The output is a matrix with updated edge weights based on gene expression similarity.

Parallel processing improves performance for large graphs.
}
\section{Functions}{
\itemize{
\item \code{calc.edge.similarityMC()}: Calculates the similarity between edges in a graph in parallel using multiple cores

\item \code{calc.edge.similarityKFOLD()}: Calculates the similarity between edges in a graph in parallel using Kfold algorithm

}}
\examples{
\dontrun{
ee <- matrix(c(1, 2, 2, 3), ncol = 2) # example edge matrix
X <- matrix(rnorm(100 * 50), 100, 50) # expression matrix
w <- calc.edge.similarity(ee, X)
}
}
