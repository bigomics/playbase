% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ai-report.R
\name{.ai_report_job}
\alias{.ai_report_job}
\title{Describe one LLM call for a report slot}
\usage{
.ai_report_job(module, slot, bp, finalize = NULL)
}
\arguments{
\item{module}{Module name the job belongs to.}

\item{slot}{Target slot under `pgx$ai`.}

\item{bp}{Built prompt, `list(system=, board=)`.}

\item{finalize}{Function applied to the generated report text, or NULL.}
}
\value{
An `ai_report_job` list.
}
\description{
A job is the unit the orchestrator hands to a runner. It deliberately holds
nothing but plain strings plus a `finalize` closure, so the two halves can be
separated: `system`/`board` are all a worker process needs (a few hundred KB,
versus the hundreds of MB a pgx costs to ship), while `finalize` stays with
the caller and never crosses a process boundary.
}
\keyword{internal}
