% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pgx-read.R
\name{read.as_matrix}
\alias{read.as_matrix}
\title{Read data file as matrix}
\usage{
read.as_matrix(file, skip_row_check = FALSE)
}
\arguments{
\item{file}{Path to input data file}

\item{skip_row_check}{(default `FALSE`) Flag to skip the removal
of empty rows}
}
\value{
Matrix object containing data from file
}
\description{
Reads a tabular data file and returns a matrix object.
Automatically detects separator and allows duplicate row names.
}
\details{
This function reads a tabular text file, automatically detecting the
separator (tab, comma, semicolon). It returns a matrix containing the data values,
using the first column as rownames (allowing duplicates). Blank rows and rows with
NA as rowname are skipped.
}
\examples{
\dontrun{
mymatrix <- read.as_matrix(mydata.csv)
}
}
